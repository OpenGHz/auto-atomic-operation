"""Task-file configuration schema models.

Split from the former ``auto_atom.framework`` monolith into per-domain modules.
This package deliberately keeps the model definitions close to their domain;
import the specific module (e.g. ``auto_atom.config.randomization``) rather than
re-exporting everything here.
"""
